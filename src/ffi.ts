

namespace Wasm {
  export const cloneRepo=(url: string,to: string): void=> undefined;//lib.symbols.clone_repo(encoder.encode(url),encoder.encode(to));
  export const close=(): void=> undefined;//lib.close;
}


export default Wasm;